import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#26499d',
        },
        secondary: {
            main: '#ffffff',
        },
        neutral: {
            main: '#000000',
        },
    },
});

export default theme;