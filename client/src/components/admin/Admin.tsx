import Users from "./Users";

/**
 * Main page for the admin interface.
 */
const Admin = () => {
    return (
        <div>
            This is the admin page
            <Users />
        </div>
    );
};

export default Admin;