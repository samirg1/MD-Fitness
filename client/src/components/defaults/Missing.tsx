import DefaultPage from "./DefaultPage";

/**
 * Page that displays when the page the user is after is not found.
 */
const Missing = () => {
    return <DefaultPage displayText="Page not found" back />;
};

export default Missing;
